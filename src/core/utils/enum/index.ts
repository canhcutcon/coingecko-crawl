// USER STATUS ENUM
export enum UserStatus {
    Enabled = "enabled",
    Removed = "removed",
}

export enum LocationUpdateStatus {
    Pending = 'pending',
    Success = 'success',
    Error = 'error'
}

